credentials:
  system:
    domainCredentials:
      - credentials:
          # Example of a credential 
          - usernamePassword:
              scope: GLOBAL
              id: "database-credentials"
              username: "${DB_USERNAME}"
              password: "${DB_PASSWORD}"
              description: "Database credentials for CI"
